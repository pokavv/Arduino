#pragma once

// Wi-Fi 인증 정보
#define WIFI_SSID      "your-wifi-ssid"
#define WIFI_PASSWORD  "your-wifi-password"

// MQTT 브로커 정보
#define MQTT_BROKER    "192.168.1.100"   // MQTT 브로커 IP
#define MQTT_USER      ""                // 사용자명 (없으면 빈 문자열)
#define MQTT_PASSWORD  ""                // 비밀번호 (없으면 빈 문자열)
