#pragma once

#define BAUD_RATE  115200

// =============================================================
// [NTP 설정]
// NTP(Network Time Protocol): 인터넷에서 정확한 시간을 가져옵니다.
// =============================================================
#define NTP_SERVER   "pool.ntp.org"   // NTP 서버 주소
#define TZ_OFFSET    (9 * 3600)       // 한국 표준시 (KST = UTC+9 = 9*3600초)
#define DST_OFFSET   0                // 한국은 서머타임 없음

// =============================================================
// [출력 간격]
// =============================================================
#define TIME_PRINT_MS           2000UL   // 현재 시각 출력 간격 (ms)
#define WIFI_CONNECT_TIMEOUT_MS 10000UL
