#pragma once

// =============================================================
// [AP(Access Point) 설정]
// ESP32가 공유기 역할을 할 때 사용하는 이름과 비밀번호
// =============================================================
#define AP_SSID      "ESP32-C3-AP"   // AP 이름 (스마트폰에서 보이는 Wi-Fi 이름)
#define AP_PASSWORD  "12345678"      // AP 비밀번호 (최소 8자리)

// =============================================================
// [통신 설정]
// =============================================================
#define BAUD_RATE  115200   // 시리얼 통신 속도

// =============================================================
// [AP 설정]
// =============================================================
#define AP_CHANNEL      1   // Wi-Fi 채널 (1~13, 기본 1)
#define AP_MAX_CLIENTS  4   // 최대 동시 접속 클라이언트 수
